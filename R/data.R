#' @title Halifax Line example mission
#' @description
#'  An example mission, which leaves the Bedford Institute of Oceanography, completes the Atlantic Zone Monitoring Program Halifax Line,
#'  and then returns to Bedford Institute of Oceaongraphy.
#'
#' @format a `data.frame` that was read in using `cruisePlanning::readCruiseInput()` with columns : `lon_dd`, `lat_dd`, `station`, `operation`,
#' `optime`, `xoptime`,`type`, `transit_speed`.
#'
"halifaxLineMission"
